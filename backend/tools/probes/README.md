Throwaway scripts from reverse engineering the Felicity Shine API (kept for reference).
Credentials come from `backend/.env`. Run from `backend/`: `.venv/bin/python tools/probes/probe_history.py`.
Endpoints that turned out to matter are wired into `app/felicity.py`.
